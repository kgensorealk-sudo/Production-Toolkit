// Thin re-export only — the real handler lives in utils/grantExtractHandler.ts and is
// shared between this route and api/ai/grant-extract.ts.
export { default, config } from '../utils/grantExtractHandler.js';
